{
  "name": "thermoloop-panel",
  "private": true,
  "scripts": {
    "build": "esbuild www/src/thermoloop-panel.js --bundle --format=esm --target=es2021 --minify --outfile=www/thermoloop-panel.js",
    "watch": "esbuild www/src/thermoloop-panel.js --bundle --format=esm --target=es2021 --minify --outfile=www/thermoloop-panel.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "lit": "^3.3.3"
  }
}
